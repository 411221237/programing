#include<stdio.h>／／缺方向
#define white 2
#include black 1
#include empty 0
int main() {
    int i,j;
    int chess[8][8]={};//輸入
    for(int _a=0; _a<8 ; _a++){
        for(int _b=0; _b<8 ; _b++){
            scanf("%d",&chess[a][b]);
        }
    }
    scanf("%d %d %d",&i,&j,&color);
    for(int x=0;x<8;x++){
        for(int y=0;y<8;y++){
            

        }
    }
    if(chess[j][i]==black){


    }

    if(chess[j][i]==empty){／／判斷位子情形
        printf("這個位子沒有棋子");

    }
    if(chess[j][i]==black){
        printf("這個位子是黑棋");
    }
    if(chess[j][i]==white){
        printf("這個位子是白棋");
    }

    
}
